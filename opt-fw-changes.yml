- name: "RHEL6 / iptables: Run firewall changes for cns to deploy"
  command: |
    -A OS_FIREWALL_ALLOW -p tcp -m state --state NEW -m tcp --dport 24007 -j ACCEPT
    -A OS_FIREWALL_ALLOW -p tcp -m state --state NEW -m tcp --dport 24008 -j ACCEPT
    -A OS_FIREWALL_ALLOW -p tcp -m state --state NEW -m tcp --dport 2222 -j ACCEPT
    -A OS_FIREWALL_ALLOW -p tcp -m state --state NEW -m multiport --dports 49152:49664 -j ACCEPT
  when: ansible_distribution == "RHEL" and ansible_distribution_major_version == "6"
- name: "RHEL7 / firewalld: Run firewall changes for cns to deploy"
  firewalld:
    state: enabled
    premanent: true
   #zone: trusted
    port: "{{ item }}"
  with_items:
    - "24007/tcp"
    - "24008/tcp"
    - "2222/tcp"
    - "49152-49664"
  when: ansible_distribution == "RHEL" and ansible_distribution_major_version == "7"
